<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.z.system.mapper.SysUserPostMapper">

    <resultMap type="SysUserPost" id="SysUserPostResult">
        <result property="userId" column="user_id"/>
        <result property="postId" column="post_id"/>
    </resultMap>

    <delete id="deleteUserPostByUserId" parameterType="Long">
        delete
        from sys_user_post
        where user_id = #{userId}
    </delete>

    <select id="countUserPostById" resultType="Integer">
        select count(1)
        from sys_user_post
        where post_id = #{postId}
    </select>

    <delete id="deleteUserPost" parameterType="Long">
        delete from sys_user_post where user_id in
        <foreach collection="array" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
    </delete>

    <insert id="batchUserPost">
        insert into sys_user_post(user_id, post_id) values
        <foreach item="item" index="index" collection="list" separator=",">
            (#{item.userId},#{item.postId})
        </foreach>
    </insert>
    <insert id="addUserCourse" parameterType="SysUserCourse" useGeneratedKeys="true" keyProperty="userCourseId">
        insert into sys_user_course(user_id, course_id) values (#{userId},#{courseId})
        <!--		<foreach item="item" index="index" collection="list" separator=",">-->
        <!--			-->
        <!--		</foreach>-->
    </insert>
    <delete id="deleteUserCourseByUserId" parameterType="Long">
        delete
        from sys_user_course
        where user_id = #{userId}
    </delete>

    <delete id="deleteUserCourse" parameterType="Long">
        delete from sys_user_course where user_id in
        <foreach collection="array" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
    </delete>
    <insert id="addUserCourseClass" parameterType="SysDept">
        insert into sys_user_course_class (user_course_id, class_id,user_id,garde_id) values (#{userCourseId},#{deptId},#{userId},#{parentId})
        <!--		<foreach item="item" index="index" collection="list" separator=",">-->
        <!--			-->
        <!--		</foreach>-->
    </insert>
    <delete id="deleteUserCourseClassByUserId" parameterType="Long">
        delete
        from sys_user_course_class
        where user_id = #{userId}
    </delete>
</mapper> 